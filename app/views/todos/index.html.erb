<h3>予定一覧</h3>
<% if flash[:notice] %>
   <p class="notice"><%= flash[:notice] %></p>
 <% end %>
<h4>本日の日付 <%= Date.current.strftime('%Y年 %m月 %d日')%></h4>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>タイトル</th>
      <th>開始日</th>
      <th>終了日</th>
      <th>終日</th>
      <th>更新日時</th>
      
    </tr>
  </thead>

  <tbody>
    <% @todos.each do |todo| %>
      <tr>
        <td><%= todo.id %></td>
        <td><%= todo.title %></td>
        <td><%= todo.start_date.to_s(:yeardate) %></td>
        <td><%= todo.end_date.to_s(:yeardate) %></td>
        <td>
        <% if todo.allday == "true" %>
        ○
        <% else todo.allday == "false" %>
        
        <% end %>
        </td>
        <td><%= todo.updated_at.to_s(:datetime_jp) %></td>
        <td><%= link_to "参照", todo %></td>
        <td><%= link_to "編集",[:edit,todo]%></td>
        <td>
          <%= link_to "削除", todo, method: :delete, data: { confirm: "本当に削除しますか？" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

  
<button><%= link_to "予定新規登録", :new_todo %></button></a>

<p>予定合数 　<span><%= @todos.ids.count %></span>件</p>
 